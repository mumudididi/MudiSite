import React from "react";
import DropdownContainer from "../DropDown/DropDownContainer";

const About = (props) => {
  return (
    <DropdownContainer>
      {props.currFocus}
      Asjkfsdklfldf
    </DropdownContainer>
  );
};

export default About;
